import { type ProjectOptions, type ProjectPublishOptions, Project } from './project.js';
export interface PackageJsonProjectOptions extends Omit<ProjectOptions, 'manifest'> {
    /**
     * Path to the package.json file
     */
    path?: string;
}
/**
 * A package.json based project.
 */
export declare class PackageJsonProject extends Project {
    /**
     * Creates a package.json based project.
     * @param options
     */
    constructor(options?: PackageJsonProjectOptions);
    publish(_options?: ProjectPublishOptions): Promise<void>;
}
//# sourceMappingURL=packageJson.d.ts.map