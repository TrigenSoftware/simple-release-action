/// <reference types="node" resolution-mode="require"/>
/// <reference types="node" resolution-mode="require"/>
export declare const changelogHeader = "# Changelog\n\nAll notable changes to this project will be documented in this file.\nSee [Conventional Commits](https://conventionalcommits.org) for commit guidelines.\n\n";
/**
 * Add release notes to a changelog file.
 * @param changelogPath - The path to the changelog file.
 * @param notes - An async iterable of strings representing the release notes.
 * @returns The release notes that were added to the changelog.
 */
export declare function addReleaseNotes(changelogPath: string, notes: AsyncIterable<string>): Promise<string>;
/**
 * Extract the last release notes from a stream.
 * @param input
 * @returns An object containing the release notes, previous tag, and next tag.
 */
export declare function extractLastRelease(input: string | string[] | Iterable<string> | AsyncIterable<string | Buffer>): Promise<{
    version: string;
    notes: string;
    previousTag: string;
    nextTag: string;
}>;
/**
 * Extract the last release notes from a changelog file.
 * @param changelogPath - The path to the changelog file.
 * @returns An object containing the release notes, previous tag, and next tag.
 */
export declare function extractLastReleaseFromFile(changelogPath: string): Promise<{
    version: string;
    notes: string;
    previousTag: string;
    nextTag: string;
} | null>;
//# sourceMappingURL=change-log.d.ts.map